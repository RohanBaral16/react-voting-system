# Development Dockerfile with hot reload
FROM node:20

WORKDIR /app

# Copy package files
COPY package*.json ./

# Install dependencies (npm install for dev, generates package-lock.json)
RUN npm install

# Copy source code
COPY . .

EXPOSE 5173

CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
